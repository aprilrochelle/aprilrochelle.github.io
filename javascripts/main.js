const { retrieveKeys, } = require('./apiKeys');

retrieveKeys();

// $(document).ready(() => {
//   $('a').each(() => {
//     if ($(this).prop('href') === window.location.href) {
//       $(this).addClass('activePg');
//     }
//   });
// });
